const name = prompt("Ismingizni kiriting:");
const spaceId = prompt("Space ID-ni kiriting:");
const spacePassword = prompt("Space parolini kiriting:");

const userInfo = {
  name: name,
  id: spaceId,
  password: spacePassword,
};

console.log(userInfo);